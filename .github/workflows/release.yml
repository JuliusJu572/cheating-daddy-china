name: Release All Platforms

permissions:
  contents: write

on:
  push:
    tags:
      - 'v*'
    branches:
      - main
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - run: npm ci
      
      - name: Build Windows
        run: npm run make -- --platform=win32
      
      - name: List Windows build output
        run: |
          echo "Windows build files:"
          Get-ChildItem -Path out -Recurse -Include *.exe,*.msi
        shell: pwsh
      
      - uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: out/make/**/*.exe
      
      # Upload artifacts only; publishing is done in a final job after all builds finish

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - run: npm ci
      
      - name: Build macOS Universal Binary
        run: npm run make -- --platform=darwin --arch=x64
      
      - name: List macOS build output
        run: |
          echo "macOS build files:"
          find out -name '*.dmg' -type f
          find out -name '*.zip' -type f
      
      - uses: actions/upload-artifact@v4
        with:
          name: macos-build
          path: |
            out/**/*.dmg
            out/**/*.zip
      
      # Upload artifacts only; publishing is done in a final job after all builds finish

  # Linux build is disabled for now
  # build-linux:
  #   if: false
  #   runs-on: ubuntu-latest

  publish-release:
    if: startsWith(github.ref, 'refs/tags/') || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: [build-windows, build-macos]
    steps:
      - uses: actions/checkout@v4

      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: windows-build
          path: dist/windows

      - name: Download macOS artifact
        uses: actions/download-artifact@v4
        with:
          name: macos-build
          path: dist/macos

      - name: Extract version from package.json
        id: pkg
        run: |
          node -e "require('fs').appendFileSync(process.env.GITHUB_ENV, 'VERSION='+require('./package.json').version+'\n')"
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ env.VERSION }}
          target_commitish: ${{ github.sha }}
          name: v${{ env.VERSION }}
          generate_release_notes: true
          files: |
            dist/windows/**/*
            dist/macos/**/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
